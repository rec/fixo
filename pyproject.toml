[project]
name = "fixo"
version = "0.1.0"
description = "Semi-automatic rule based editing of Python code"
readme = "README.md"
requires-python = ">=3.10"

[dependency-groups]
dev = [
    "mypy>=1.18.2",
    "pyright>=1.1.407",
    "pytest>=8.4.2",
    "ruff>=0.14.3",
]
