[project]
name = "fixo"
version = "0.1.0"
description = "Semi-automatic rule based editing of Python code"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mypy>=1.17.1",
    "pyright>=1.1.406",
    "pytest>=8.4.2",
    "ruff>=0.12.12",
    "typer>=0.17.3",
]
